import os

group_reserch_hadith_id = int(os.getenv("RESERCH_HADITH") , 0)
group_reserch_clip_id = int(os.getenv("RESERCH_CLIP_ID"), 0)
group_reserch_lecture_id = int(os.getenv("RESERCH_LECTURE_ID"), 0)

bale_channel_id = int(os.getenv("CHANNEL_BALE"), 0)
eitaa_channel_id = int(os.getenv("CHANNEL_EITAA"), 0)
eitaa_channel_id_test = int(os.getenv("CHANNEL_EITAA_TEST"), 0)
